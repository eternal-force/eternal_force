# 需求矩陣表

追蹤功能需求與其實作範圍、驗收標準的對應關係，供日後回溯「為什麼這樣做」與「改動影響哪些檔案」使用。
新需求請依序附加在表格最後，編號遞增、不重複使用舊編號。

| 編號 | 需求描述 | 提出日期 | 影響範圍 | 驗收標準 | 狀態 |
|---|---|---|---|---|---|
| REQ-001 | 新增上課紀錄表單移除「上課時長」欄位 | 2026-08-26 | `app/models.py`(`ClassRecord`)、`app/forms.py`、`app/services.py`、`app/classes/routes.py`、`app/templates/classes/form.html`、`app/templates/students/detail.html` | 新增/編輯上課紀錄表單不再出現「上課時長」欄位；上課紀錄列表不再顯示時長欄位；既有資料庫欄位保留(未刪除)以避免破壞既有資料，僅前端與 API 不再讀寫。 | 已完成 |
| REQ-002 | 新增上課紀錄表單新增「上課教練」欄位，從角色為教練(coach)且啟用中的帳號以下拉選單選擇，為必填 | 2026-08-26 | `app/models.py`(`ClassRecord.coach_id`)、`app/forms.py`(`ClassRecordForm.coach_id`)、`app/services.py`(`list_active_coaches`、`_validate_coach`)、`app/classes/routes.py`、`app/templates/classes/form.html`、`app/templates/students/detail.html` | 新增/編輯上課紀錄表單顯示教練下拉選單，選項來源為 `role='coach'` 且 `status='active'` 的帳號；未選擇教練時顯示「請選擇上課教練」並擋下送出；後端(HTML 表單與 `/api` JSON 端點)皆會驗證教練 id 合法性，避免竄改請求繞過。 | 已完成 |
| REQ-003 | 訓練菜單新增方式參考 CMG 專案(`C:\Users\HW-X54\Projects\CMG`)的訓練項目編輯器，項目需可填公斤數/組數/次數/備註，且項目填寫完成後可收合以節省畫面空間 | 2026-08-26 | `app/models.py`(`ClassExercise.weight_kg/sets/reps/note`)、`app/services.py`(`_build_class_exercises` 數值驗證)、`app/classes/routes.py`、`app/templates/classes/_exercise_row.html`(新增)、`app/templates/classes/form.html`、`app/static/js/main.js`、`app/static/css/style.css`、`app/templates/students/detail.html` | 訓練菜單每個項目可填公斤數/組數/次數/備註(皆選填，但填了必須是非負數字，否則顯示錯誤訊息並擋下送出)；每個項目列可點「收合」摘要顯示(分類·名稱·組數x次數·公斤數)，點列頭可再展開編輯；編輯模式載入既有項目時預設以收合狀態顯示；新增下一個項目時，已填寫名稱的前一列會自動收合。 | 已完成 |
| REQ-004 | 帳號停用時，需連同該帳號對應的學生名冊狀態一併改為「停用」，避免帳號已停用但學生名冊仍顯示在籍 | 2026-09-04 | `app/services.py`(`set_account_status`) | 管理者在帳號管理將某帳號狀態改為「停用」且該帳號有連結學生名冊(`user.student_id` 不為空)時，對應 `Student.status` 會同步變更為 `inactive`；重新啟用帳號不會反向自動變更學生名冊狀態(僅處理停用方向，避免誤將原本刻意設為停用的學生名冊喚醒)。 | 已完成 |
| REQ-005 | 帳號狀態為「停用」時，帳號管理列表與帳號詳情頁需出現「刪除」按鈕；點擊刪除時，若該帳號對應的學生名冊仍有剩餘課程(`remaining > 0`)，禁止刪除並提示「該學生尚有課程未完成，請勿刪除」 | 2026-09-04 | `app/services.py`(新增 `delete_account`)、`app/accounts/routes.py`(新增 `DELETE /accounts/<id>/delete` 路由)、`app/templates/accounts/list.html`、`app/templates/accounts/detail.html` | 僅「已停用」且非管理者角色的帳號會顯示「刪除」按鈕(帳號管理列表與帳號詳情頁皆有)；點擊後跳出確認對話框，確認後送出：若對應學生名冊 `remaining > 0`，顯示 danger flash 訊息「該學生尚有課程未完成，請勿刪除」且不刪除任何資料；若無對應學生名冊或 `remaining <= 0`，則刪除該帳號，且一併刪除對應學生名冊(含其購買/上課紀錄，透過既有 cascade 規則)；管理者(`admin`)角色帳號一律無法刪除。 | 已完成 |
| REQ-006 | 帳號管理欄位「手機」改名為「連絡電話」，並新增下拉選單選擇「手機」或「市話」；選手機時需符合手機格式(09 開頭 10 碼數字)，選市話時需符合市話格式(區碼+電話號碼)，格式不符擋下送出 | 2026-09-04 | `app/models.py`(`User.phone_type`)、`app/services.py`(`validate_phone_by_type`、`PHONE_TYPE_LABELS`，`register_student_account`／`create_coach_account` 套用驗證)、`app/forms.py`(`PHONE_TYPE_CHOICES`、`RegisterForm`／`CoachForm` 新增 `phone_type` 欄位並改標籤為「連絡電話」)、`app/auth/routes.py`、`app/accounts/routes.py`、`app/templates/auth/register.html`、`app/templates/accounts/new_coach.html`、`app/templates/accounts/detail.html`、`tests/test_auth.py`、`tests/test_accounts.py` | 「新增教練帳號」與「學生註冊」表單皆改為「連絡電話」欄位並新增「電話類型」下拉選單(手機/市話)；選手機時電話需符合 `^09\d{8}$`，選市話時需符合 `^(?!09)0\d{1,2}-?\d{6,8}$`(區碼 1~2 碼、電話 6~8 碼，可加「-」，且排除手機開頭的 09 以避免與手機格式混淆)，不符合時該欄位顯示錯誤訊息並擋下送出；帳號詳情頁「連絡電話」會一併顯示所選類型(手機/市話)；`pytest` 全數 96 項測試通過。 | 已完成 |
| REQ-007 | 學生列表(學生詳情頁)點擊「停用」後，下方「+ 新增購買紀錄」「+ 新增上課紀錄」按鈕需隱藏；並需有「重新啟用」按鈕可將學生改回在籍 | 2026-09-04 | `app/templates/students/detail.html` | 學生 `status == 'inactive'` 時，購買紀錄區塊的「+ 新增購買紀錄」與上課紀錄區塊的「+ 新增上課紀錄」按鈕不顯示；`status == 'active'` 時兩按鈕維持顯示(角色仍需為 admin/coach)；頁面頂部原有的狀態切換按鈕(`students.change_status`)在停用狀態下顯示為「重新啟用」，點擊後可改回在籍狀態，此按鈕為既有功能(不受本次調整影響，仍持續顯示)。 | 已完成 |
| REQ-008 | 學生登入自己的帳號後，一樣可以點擊「編輯資料」編輯自己的學生名冊資料，但無法修改角色 | 2026-09-04 | `app/students/routes.py`(`edit_student` 開放給 `student` 角色存取，但僅限本人)、`app/templates/students/detail.html`(「編輯資料」連結對所有已登入使用者顯示，不再限定 admin/coach)、`app/templates/students/form.html`(在籍狀態欄位)、`tests/test_students.py` | 學生登入後可看到並點擊自己學生詳情頁的「編輯資料」，成功進入 `/students/<id>/edit`（`GET` 200）並可修改姓名/聯絡電話/信箱/生日/性別/入班日期/備註；嘗試存取「非本人」的學生編輯頁一律回傳 403；表單不會出現可編輯的「在籍狀態」下拉選單(改以隱藏欄位帶出現值)，即使在送出時竄改該欄位的值，後端(`edit_student` 路由)仍會忽略學生角色送出的 `status`，資料庫中的狀態不會被改動；admin/coach 編輯畫面不受影響，仍可正常修改在籍狀態。 | 已完成 |
| REQ-009 | 學生詳情頁「購買紀錄」列表新增表頭：購買日期、購買堂數、購買金額、備註 | 2026-09-04 | `app/templates/students/detail.html` | 購買紀錄區塊在資料列上方顯示表頭列(購買日期／購買堂數／購買金額／備註／操作)；沿用既有 `.record-row`／`.record-row-header` 樣式，寬螢幕(≥640px)顯示表頭列、隱藏各欄位前綴的行動版標籤(`field-label-mobile`)，窄螢幕則相反(隱藏表頭、改以每欄前綴文字標示)；「操作」表頭欄僅在 admin/coach 檢視時顯示文字(呼應該欄操作按鈕僅 admin/coach 可見的既有規則)；行動版欄位標籤文字同步從「堂數」「金額」調整為「購買堂數」「購買金額」，與新表頭用詞一致。 | 已完成 |
| REQ-010 | 購買紀錄之購買堂數上限為 9999，購買金額上限為 1,000,000，超過時擋下送出 | 2026-09-12 | `app/forms.py`(`PurchaseForm.quantity`/`price`)、`app/services.py`(`_validate_positive_int`、新增 `_validate_purchase_price`、`create_purchase`、`update_purchase`)、`app/templates/purchases/form.html`、`tests/test_purchases.py` | 新增/編輯購買紀錄時，「購買堂數」大於 9999 或「購買金額」大於 1,000,000 皆顯示對應錯誤訊息(「購買堂數不能大於 9999」／「購買金額不能大於 1,000,000」)並擋下送出；剛好等於上限值(9999／1,000,000)可正常送出成功；驗證邏輯寫在共用服務層(`services.py`)，HTML 表單與 `/api/*` JSON 端點皆會套用，避免規則兜不起來。 | 已完成 |
| REQ-011 | 學生列表卡片中，若該學生剩餘堂數小於 5 堂，需顯示文字提醒教練及早安排續購 | 2026-09-12 | `app/templates/students/list.html`、`tests/test_students.py` | 學生列表卡片中，`remaining < 0`(已超上)維持顯示既有「已超上，請儘快補購」危險樣式提示；`0 <= remaining < 5` 時改顯示新增的「剩餘堂數不足 5 堂，請提醒教練安排續購」提醒樣式文字；`remaining >= 5` 時不顯示任何提醒。 | 已完成 |
| REQ-012 | 學生註冊帳號經管理者「拒絕」(停用)後，若再次「重新啟用」該帳號，需把帳號與學生名冊重新綁定在一起(建立對應學生名冊資料並連結) | 2026-09-12 | `app/services.py`(`set_account_status`) | 學生角色帳號於待驗證(`pending`)狀態被「拒絕」(呼叫 `change_status` 設為 `disabled`，此時 `student_id` 仍為 `None`，尚未建立學生名冊)後，管理者再次點擊「重新啟用」(設回 `active`)時，會比照「驗證」動作呼叫 `create_student_from_registration` 建立學生名冊並寫入 `user.student_id`，使帳號與新建立的學生名冊重新綁定；已綁定學生名冊的帳號(`student_id` 不為 `None`)在停用/重新啟用之間切換不受影響，不會重複建立學生名冊。 | 已完成 |
| REQ-013 | 學生列表(學生詳情頁)將學生「重新啟用」時，若該學生有對應的登入帳號，該帳號也需一併改為啟用 | 2026-09-12 | `app/services.py`(新增 `_activate_linked_account`、`set_student_status`) | 在學生列表／詳情頁點擊「重新啟用」(呼叫 `students.change_status` → `set_student_status(student, "active")`)時，若該學生有連結的登入帳號(`User.student_id` 對應)且該帳號狀態不是 `active`(例如先前依 REQ-004 被連動停用)，則一併將該帳號狀態改為 `active`；管理者(`admin`)角色帳號不受影響(理論上不會被連結，僅作防呆)；學生狀態改為 `inactive`(停用)方向不受本次調整影響，維持既有規則(僅帳號停用會連動學生名冊，反向不會自動連動)。 | 已完成 |
| REQ-014 | 用學生角色登入並檢視自己的學生詳情頁時，隱藏「回學生列表」連結(因學生列表對學生角色會直接導回自己的詳情頁，點了等於沒作用) | 2026-09-14 | `app/templates/students/detail.html` | 學生角色檢視自己的學生詳情頁時，頁面上方不出現「回學生列表」連結；admin/coach 檢視任何學生詳情頁時，該連結維持顯示不受影響。 | 已完成 |
| REQ-015 | 教練/管理者輸入購買堂數／購買金額／訓練菜單公斤數／組數時，需限制數值上限：購買堂數 999、購買金額 1,000,000、公斤數 200、組數 100(取代 REQ-010 原訂的購買堂數上限 9999，改為 999) | 2026-09-14 | `app/forms.py`(`PurchaseForm.quantity`)、`app/services.py`(`_validate_positive_int` 呼叫改為 `max_value=999`、`_parse_optional_decimal`/`_parse_optional_int` 新增 `max_value` 參數並套用於公斤數/組數)、`app/templates/purchases/form.html`、`app/templates/classes/_exercise_row.html`、`tests/test_purchases.py`、`tests/test_classes.py` | 新增/編輯購買紀錄時，購買堂數大於 999 會顯示「購買堂數不能大於 999」並擋下送出(原上限 9999 已不再生效)，剛好 999 可成功送出；新增/編輯上課紀錄的訓練菜單項目中，公斤數大於 200 顯示「公斤數不能大於 200」、組數大於 100 顯示「組數不能大於 100」並擋下送出，剛好等於上限可成功送出；次數(reps)不在本次限制範圍內，維持原本只需為非負整數的規則；驗證邏輯位於共用服務層，HTML 表單與 `/api/*` JSON 端點皆套用。 | 已完成 |
| REQ-016 | 學生列表卡片姓名下方顯示該學生對應的登入帳號(字體比姓名小)；學生姓名被更新時，帳號管理頁與學生列表顯示的姓名需同步 | 2026-09-14 | `app/models.py`(`User.student` 關聯新增 `backref="account"`)、`app/services.py`(`update_student` 同步更新 `student.account.name`)、`app/templates/students/list.html`、`tests/test_students.py` | 學生列表卡片中，若該學生有對應登入帳號，姓名下方以較小字體(`.muted`，0.75rem)顯示該帳號的使用者名稱；無對應帳號則不顯示。編輯學生資料變更姓名且該學生有連結帳號時，連結帳號的 `User.name` 會同步更新為新姓名，帳號管理頁(`accounts/detail.html`)、學生列表/詳情頁後續讀取到的姓名皆為更新後的新姓名；無連結帳號的學生變更姓名不受影響(僅更新 `Student.name`)。 | 已完成 |
| REQ-017 | 新增/編輯學生訓練菜單所選用的訓練項目，若已被(其他)學生的訓練菜單引用，則該訓練項目清單項目不得再被編輯或刪除 | 2026-09-14 | `app/services.py`(新增 `_check_exercise_catalog_item_not_in_use`、套用於 `update_exercise_catalog_item`／`delete_exercise_catalog_item`)、`app/exercises/routes.py`(`delete_exercise` 補上例外處理)、`app/api/routes.py`(`api_delete_exercise_catalog_item` 補上例外處理)、`tests/test_exercises.py` | 「訓練項目清單」管理頁面中，若某訓練項目已被至少一筆上課紀錄的訓練菜單引用(`ClassExercise.exercise_catalog_item_id` 對應)，嘗試編輯(改分類/名稱)或刪除該項目時，會顯示「此訓練項目已被學生的訓練菜單引用，無法編輯/刪除」並擋下動作，資料不會被變更；未被任何訓練菜單引用的項目編輯/刪除不受影響；HTML 路由與 `/api/*` JSON 端點皆套用同一規則。 | 已完成 |
| REQ-018 | 教練角色登入時，隱藏帳號管理列表頁與學生詳情頁的「停用」按鈕(僅隱藏 UI，非後端權限限制) | 2026-09-14 | `app/templates/accounts/list.html`、`app/templates/students/detail.html`、`tests/test_accounts.py`、`tests/test_students.py` | 教練登入帳號管理列表頁時，啟用中且非管理者的帳號列不會出現「停用」按鈕(僅管理者可見)；教練登入在籍學生的詳情頁時，不會出現「停用」按鈕，但學生已停用時「重新啟用」按鈕仍正常顯示；管理者(admin)登入時，以上兩處「停用」按鈕皆不受影響、正常顯示；帳號管理頁的「拒絕」(針對待驗證帳號)與「重新啟用」按鈕不在本次調整範圍內，教練仍可操作。 | 已完成 |
| REQ-019 | 教練/管理者輸入上課紀錄訓練菜單的「次數」時，需限制上限為 100(呼應 REQ-015 已限制的公斤數 200、組數 100) | 2026-09-14 | `app/services.py`(`_build_class_exercises` 呼叫 `_parse_optional_int(..., "次數", max_value=100)`)、`app/templates/classes/_exercise_row.html`、`tests/test_classes.py` | 新增/編輯上課紀錄的訓練菜單項目中，次數大於 100 顯示「次數不能大於 100」並擋下送出，剛好等於 100 可成功送出；驗證邏輯位於共用服務層，HTML 表單與 `/api/*` JSON 端點皆套用。 | 已完成 |
| REQ-020 | 帳號管理的帳號檢視(詳情)頁新增顯示「信箱」欄位 | 2026-09-14 | `app/models.py`(`User.email`，新增欄位)、`app/forms.py`(`RegisterForm`／`CoachForm` 新增 `email` 欄位)、`app/services.py`(`register_student_account`／`create_coach_account` 寫入 `email`)、`app/templates/auth/register.html`、`app/templates/accounts/new_coach.html`、`app/templates/accounts/detail.html`、`tests/test_auth.py`、`tests/test_accounts.py` | 「學生註冊」與「新增教練帳號」表單新增「信箱」欄位(選填，格式需符合 Email 格式，否則顯示錯誤訊息並擋下送出)；帳號管理的帳號詳情頁在「姓名」下方新增顯示「信箱」欄位(無填寫則顯示「-」)。`User.email` 為新增欄位，已直接對目前連線中的 Supabase 資料庫執行 `ALTER TABLE public.users ADD COLUMN IF NOT EXISTS email VARCHAR(255)`；換資料庫或重建環境時需比照手動補跑(呼應 REQ-006 補充說明的既有做法)。 | 已完成 |
| REQ-021 | 學生詳情頁除了顯示手機、備註外，也需顯示對應的登入帳號；並針對「手機」與「帳號」欄位加上欄位名稱標籤，以利分辨 | 2026-09-14 | `app/templates/students/detail.html`、`tests/test_students.py` | 學生詳情頁頂部原本未加標籤、直接顯示手機號碼的地方改為「手機：{電話}」；若該學生有連結的登入帳號，手機/信箱那行下方新增顯示「帳號：{帳號使用者名稱}」；無連結帳號則不顯示該行；「備註」欄位維持既有的「備註：」標籤不變。 | 已完成 |
| REQ-022 | 學生姓名新增/編輯時需限制格式：不得包含特殊符號、字數不得超過 20 個字 | 2026-09-14 | `app/services.py`(新增 `validate_student_name`，套用於 `create_student`／`update_student`)、`app/forms.py`(`StudentForm.name` 新增 `_validate_student_name_field` 驗證器)、`app/students/routes.py`(`new_student`／`edit_student` 補上例外處理)、`tests/test_students.py`、`tests/test_security.py` | 新增/編輯學生時，姓名只允許中文(CJK 統一表意文字)、英文字母、數字與空白，包含其他符號(標點、特殊字元等，含 `<script>` 等 XSS 常見 payload)一律顯示「姓名不能包含特殊符號」並擋下送出；姓名超過 20 個字顯示「姓名不能超過 20 個字」並擋下送出，剛好 20 字可成功送出；驗證邏輯位於共用服務層，HTML 表單與 `/api/*` JSON 端點皆套用；`tests/test_security.py` 原本測試「姓名含 `<script>` 標籤會被逃逸顯示」的案例已改為測試「直接被擋下」，另外新增以「備註」欄位驗證 Jinja autoescape 仍正常運作(備註欄位不受本次姓名格式限制影響)。 | 已完成 |
| REQ-023 | 帳號管理的學生帳號從「停用」改為「啟用」時，對應的學生名冊也需一併改為在籍(補上 REQ-004 當初刻意不做的反向同步) | 2026-09-14 | `app/services.py`(`set_account_status`) | 管理者在帳號管理將某學生角色帳號的狀態從「停用」改為「啟用」時，若該帳號有連結的學生名冊，`Student.status` 會同步變更為 `active`；此為 REQ-004 原「重新啟用帳號不會反向自動變更學生名冊狀態」規則的明確變更(需求方確認即使學生名冊是因其他原因被設為停用，帳號重新啟用時仍要一併喚醒)，REQ-004 表格內文字保留原始歷史記錄不回頭改字，以此補充說明為準；停用方向規則不受影響。 | 已完成 |
| REQ-024 | 相同帳號(使用者名稱)不得重複註冊或新增 | 2026-09-14 | `app/services.py`(新增 `username_exists`，供 `register_student_account`／`create_coach_account` 使用)、`app/forms.py`(`_validate_username_unique` 改用 `username_exists`) | 實測發現既有的帳號重複檢查(`User.query.filter_by(username=...)`)為大小寫敏感比對，導致「Alice」與「alice」會被當成兩個不同帳號，可重複註冊/新增，屬於既有規則的漏洞。修正後改以 `db.func.lower(username) == 大小寫正規化後的輸入值` 比對，確保「學生自助註冊」與「帳號管理新增教練帳號」在使用者名稱僅大小寫不同的情況下，也會顯示「此帳號已被使用」並擋下建立；完全相同大小寫的重複註冊/新增原本就已擋下，不受影響。 | 已完成 |
| REQ-025 | 從卡片(學生列表/詳情頁)編輯學生資料時，「連絡電話」欄位需比照學生自助註冊的做法，新增「電話類型」(手機/市話)下拉選單並套用對應格式驗證；學生明細頁需依電話類型顯示「市話」或「手機」標籤 | 2026-09-14 | `app/models.py`(`Student.phone_type`，新增欄位)、`app/forms.py`(`StudentForm` 新增 `phone_type` 欄位，`phone` 改套用 `_validate_phone_field`)、`app/services.py`(`create_student`／`update_student` 驗證並寫入 `phone_type`；`create_student_from_registration` 一併複製 `user.phone_type`)、`app/students/routes.py`、`app/templates/students/form.html`、`app/templates/students/detail.html`、`tests/test_students.py`、`tests/test_api.py` | 新增/編輯學生資料表單新增「電話類型」下拉選單(手機/市話)，選手機時電話需符合 `^09\d{8}$`、選市話時需符合 `^(?!09)0\d{1,2}-?\d{6,8}$`(與 REQ-006 的 `validate_phone_by_type` 共用同一組規則)，格式不符或有電話卻未選類型時擋下送出；學生明細頁電話欄位依 `student.phone_type` 顯示「市話：{電話}」或「手機：{電話}」(未設定類型預設顯示「手機」)；學生自助註冊建立的學生名冊(`create_student_from_registration`)會直接沿用註冊時所選的電話類型，不需要另外在學生名冊補填。`Student.phone_type` 為新增欄位，已直接對目前連線中的 Supabase 資料庫執行 `ALTER TABLE public.students ADD COLUMN IF NOT EXISTS phone_type VARCHAR(20)`；換資料庫或重建環境時需比照手動補跑(呼應 REQ-006／REQ-020 的既有做法)。 | 已完成 |
| REQ-026 | 隱藏學生列表頁面上的「學生列表」標題文字 | 2026-09-14 | `app/templates/students/list.html` | 學生列表頁面(`/students/`)不再顯示「學生列表」的頁面標題(`<h2>`)，僅保留搜尋/篩選列與「+ 新增學生」按鈕；瀏覽器分頁標題(`<title>`)與導覽列選單的「學生列表」連結文字不受影響，仍維持顯示。 | 已完成 |
| REQ-027 | 學生明細頁的「備註」改為顯示「期望運動達成的效益」，且不重複顯示 | 2026-09-14 | `app/forms.py`(`StudentForm.notes` 標籤改為「期望運動達成的效益」)、`app/services.py`(`create_student_from_registration` 移除寫入 `notes` 時內建的「期望運動達成效益：」前綴文字)、`app/templates/students/detail.html` | 學生明細頁原本顯示「備註：{內容}」之處改為顯示「期望運動達成的效益：{內容}」；學生編輯表單的「備註」欄位標籤同步改為「期望運動達成的效益」，兩處用詞一致。修正前，學生自助註冊建立的學生名冊，`notes` 欄位會被寫入帶有「期望運動達成效益：」前綴的字串，明細頁再加上「備註：」標籤，會顯示成「備註：期望運動達成效益：xxx」的重複用詞；修正後 `notes` 只存使用者填寫的原始內容，搭配新標籤只會顯示一次「期望運動達成的效益：xxx」。管理者/教練直接新增學生或編輯既有學生的「期望運動達成的效益」欄位，本來就只存原始輸入內容，不受影響。 | 已完成 |
| REQ-028 | 學生列表的搜尋/篩選介面需與帳號管理一致，且查詢條件區塊可縮合，縮合方式須符合 RWD 設計 | 2026-09-14 | `app/templates/students/list.html`、`app/templates/accounts/list.html`、`app/static/css/style.css`(`.filter-bar`) | 學生列表與帳號管理的搜尋列皆改為可收合的 `<details class="card">` 區塊(比照學生詳情頁「購買紀錄」等區塊的既有收合樣式，點擊標題列可展開/收合，預設為展開狀態)，收合／展開為原生 HTML5 `<details>`/`<summary>` 行為，不需額外 JavaScript，在窄螢幕與寬螢幕皆可正常運作；同時修正 `.filter-bar` CSS 原本寫死三欄(`grid-template-columns: 2fr 1fr auto`)僅適用學生列表(姓名搜尋＋狀態＋按鈕)的問題，改為可彈性換行的版面(小螢幕單欄堆疊，≥560px 改為同一列並自動換行)，讓欄位數不同的帳號管理搜尋列(僅帳號/姓名搜尋＋按鈕)與學生列表搜尋列都能正確對齊，兩者的搜尋列 UI 呈現一致。 | 已完成 |
| REQ-029 | 隱藏學生列表頁面的「新增學生」按鈕，但學生列表本身(搜尋/篩選、學生卡片、分頁)維持正常顯示 | 2026-09-14 | `app/templates/students/list.html`、`tests/test_students.py` | 學生列表頁面(`/students/`)不再顯示「+ 新增學生」按鈕，其餘內容(搜尋/篩選收合區塊、學生卡片列表、分頁)不受影響；清單為空時的提示文字同步移除「點選『新增學生』開始建立」的過時引導文案，改為單純的「目前還沒有學生資料。」。本項僅隱藏此頁面的 UI 入口，後端 `students.new_student`(`/students/new`)路由本身未停用(例如透過網址直接存取仍可使用)；若需要一併停用路由或改為僅能由學生自助註冊建立，需另外調整 `app/students/routes.py`。 | 已完成 |
| REQ-030 | 所有「家電」用詞統一改為「市話」 | 2026-09-14 | `app/forms.py`(`PHONE_TYPE_CHOICES`)、`app/services.py`(`PHONE_TYPE_LABELS`、`validate_phone_by_type` 錯誤訊息與註解)、`app/templates/accounts/detail.html`、`app/templates/students/detail.html`、`tests/test_students.py` | 全站所有原本顯示「家電」的地方(電話類型下拉選單選項、格式錯誤訊息、帳號/學生明細頁的電話類型標籤)皆改為「市話」，行為與驗證規則不變(僅文字用詞調整)。 | 已完成 |
| REQ-031 | 學生登入後編輯自己的資料時，「聯絡信箱」欄位需改名為「信箱」，並自動帶入該學生目前的信箱值 | 2026-09-14 | `app/forms.py`(`StudentForm.email` 標籤改為「信箱」)、`app/services.py`(`create_student_from_registration` 補上 `email=user.email`)、`tests/test_students.py` | 學生編輯資料表單(含學生角色自行編輯與 admin/coach 編輯)的信箱欄位標籤統一改為「信箱」；實測發現學生自助註冊時填寫的信箱只存進 `User.email`，建立對應學生名冊(`create_student_from_registration`)時未一併複製到 `Student.email`，導致學生登入後編輯自己資料時信箱欄位是空的，屬於既有邏輯的漏洞。修正後註冊建立學生名冊時會一併帶入註冊信箱，编輯表單開啟時(`StudentForm(obj=student)`)即可正確帶出已填寫的信箱值以利編輯更新；已對正式 Supabase 資料庫執行資料回補，將既有 2 筆「帳號有信箱、但學生名冊信箱是空的」的資料同步更新為帳號上的信箱值。 | 已完成 |

## 補充說明

- REQ-001：資料庫的 `class_records.duration_minutes` 欄位刻意保留未刪除，因專案目前僅用 `flask init-db`(`db.create_all()`)建表，沒有 Alembic / Flask-Migrate 等版本化遷移工具，刪除欄位需要手動 `ALTER TABLE`，風險與效益不成比例，故僅停用讀寫、不動既有 schema。
- REQ-002／REQ-003：這兩項變更皆對既有的 Supabase(PostgreSQL)資料表新增欄位(`class_records.coach_id`、`class_exercises.weight_kg`/`sets`/`reps`/`note`)。由於同樣沒有遷移工具，已直接對目前連線中的資料庫執行等冪的 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`；若日後切換到新的資料庫(例如正式環境 Render 上的 Supabase 專案)，`flask init-db` 建立的全新資料表會直接包含這些欄位，但**既有**資料庫仍需比照手動補跑一次相同的 `ALTER TABLE`陳述式，否則會出現「欄位不存在」的錯誤。長期建議依 README 既有規劃導入 Flask-Migrate / Alembic，避免每次改 schema 都要手動介入。
- 上課教練下拉選單目前資料來源是「角色為教練且帳號狀態為啟用中」的使用者；若系統中尚無任何教練帳號，選單會是空的，需先透過「帳號管理 → 新增教練」建立至少一位教練帳號，新增上課紀錄功能才可用。
- REQ-008：需求原文提到學生編輯自己資料時「無法修改角色」，但「角色」(`role`：admin/coach/student)是 `User` 帳號的屬性，`students/edit` 這個表單編輯的是 `Student`(學生名冊)資料，本來就不含角色欄位、也從未讓使用者在此表單變更角色。表單中唯一具治理性質、對應「身份/資格」的欄位是「在籍狀態」(`status`：在籍/停用)，因此依此推斷需求實際指的是這個欄位，並依此實作限制；若之後發現需求其實是指別的欄位或別的頁面，請再告知以修正。
- REQ-005：刪除帳號會**連同對應的學生名冊一併硬刪除**(而非僅解除連結)，因為需求描述本身即以「該帳號對應的學生列表是否還有剩餘課程」作為是否允許刪除的判斷依據——若刪除帳號後學生名冊仍保留，這個檢查就沒有意義。若日後只想刪除登入帳號、保留學生歷史資料，需另外調整此邏輯(可參考既有 `delete_student_hard` 僅解除連結、不刪學生的做法)。
- REQ-006：`User.phone_type` 為新增欄位，比照 REQ-002／REQ-003 的做法，已直接對目前連線中的 Supabase 資料庫執行 `ALTER TABLE public.users ADD COLUMN IF NOT EXISTS phone_type VARCHAR(20)`；換資料庫或重建環境時需比照手動補跑。電話格式驗證邏輯統一寫在 `app/services.py` 的 `validate_phone_by_type`，表單(`app/forms.py`)透過共用函式呼叫，避免表單與服務層規則兜不起來(呼應本檔開頭「HTML 路由與 JSON API 路由都呼叫服務層」的既有原則)。目前僅套用在「帳號管理」建立教練帳號與學生自助註冊這兩個會寫入 `User.phone`/`phone_type` 的表單；「學生名冊」(`Student.phone`，`app/templates/students/form.html`)為獨立欄位與模組，不在本次「帳號管理」需求範圍內，故未變動。帳號詳情頁(`accounts/detail.html`)目前僅唯讀顯示連絡電話，尚無「編輯既有帳號連絡電話」的頁面/表單，此需求未提及故未新增。
- REQ-015：購買堂數上限由 REQ-010 訂定的 9999 調整為 999，屬於同一欄位限制的後續修正(需求方澄清實際要的上限值)，REQ-010 表格內文字保留原始歷史記錄不回頭改字，以此補充說明為準。
- REQ-018：本項僅在樣板(Jinja)層隱藏「停用」按鈕的顯示，對應的後端路由(`accounts.change_status`、`students.change_status`)權限仍維持 `roles_required("admin", "coach")`不變，教練若直接以工具送出 POST 請求仍可執行停用動作；若日後需要在後端也禁止教練停用帳號/學生，需額外調整對應路由或服務層的角色檢查(需一併確認是否會影響教練「拒絕」待驗證帳號的既有功能，因兩者共用同一支 `change_status`／`set_account_status`)。
- REQ-022：允許的字元集為中文(CJK 統一表意文字，Unicode `一`-`鿿`)、英文字母、數字與空白；不含中日韓標點、全形符號、底線等其他字元。若之後發現有合法姓名包含目前規則未涵蓋的字元(例如商標符號、破折號分隔的複姓)，需要再放寬 `services.py` 的 `_STUDENT_NAME_RE`。
- REQ-021／REQ-027：REQ-021 當時寫的驗收標準提到「備註」欄位標籤維持不變，該敘述已被 REQ-027 取代(備註標籤改為「期望運動達成的效益」)，REQ-021 表格內文字保留原始歷史記錄不回頭改字，以 REQ-027 的內容為準。
- REQ-024：目前的重複帳號檢查屬於「查詢後再寫入」(check-then-insert)的應用層檢查，與既有(修正前)的完全相同大小寫檢查一樣，理論上在極端的高併發情況下仍存在極小的競態窗口(兩個請求幾乎同時通過檢查)；資料庫的 `users.username` 唯一索引仍是大小寫敏感，並未加上大小寫不敏感的資料庫層級唯一約束。若要徹底杜絕，需額外建立如 `CREATE UNIQUE INDEX ON users (lower(username))` 的運算式索引，屬於資料庫結構調整，暫不在本次範圍內。
